export default function details({params}:any){
    return <div>
        <h1>Abot details: {params.slug[0]}</h1>
        <h1>Abot details: {params.slug[1]}</h1>
        <h1>Abot details: {params.slug[2]}</h1>

    </div>
}